<view class="">
  <!-- 轮播图 -->
  <swiper class="swiper" indicator-dots autoplay circular>
    <block 
      wx:for="{{goodsList}}" 
      wx:key="pics_id"
      >
      <swiper-item>
        <image class="" src="{{item.pics_mid_url}}" mode=""></image>
      </swiper-item>
    </block>
  </swiper>
</view>
